/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
        domains: [
            "www.ifms.edu.br",
            "www.ifms.ead.edu.br"
        ]
    }
};

export default nextConfig;
